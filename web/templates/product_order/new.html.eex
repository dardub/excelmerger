<h2>New product order</h2>

<%= render "form.html", changeset: @changeset,
                        action: product_order_path(@conn, :create) %>

<%= link "Back", to: product_order_path(@conn, :index) %>
